# Daily Coach Site Integration

This folder is prepared for the `dailycoach/LCMS` GitHub Pages site.

- Keep the root `index.html` for the new main page.
- Put the existing LCMS image carousel at `/lcms/`.
- Put card tools under `/cards/...`.
- Put diagnosis and coaching tools under `/tests/...`.
- Use `pages.json` as the menu/source list for the main page.

Suggested copy target: the root of the `dailycoach/LCMS` repository on a new branch.
