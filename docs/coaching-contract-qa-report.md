# DAILYCOACHING 코칭계약 시스템 QA 보고서

기준 브랜치: `agent/coaching-guide-contracts`
기준 main SHA: `3f00e72fe6f7ca0164d48689c0890d2a61396a79`
검수일: 2026-07-30
상태: 정적 프런트와 Apps Script 소스 패키지 검수 완료 · 실제 Google 운영 연동 전

## 1. 자동검수

| 명령 | 결과 |
|---|---|
| `node scripts/check-coaching-contracts.mjs` | 통과 · 153/153 |
| 공개 JavaScript `node --check` | 통과 |
| Apps Script `.gs`·HTML inline script 구문검사 | 통과 |
| `appsscript.json`·`pages.json` JSON 파싱 | 통과 |
| `npx html-validate` 신규 공개 HTML 4개 | 통과 |
| `git diff --check` | 통과 |

정적 검수는 필수 파일·메뉴 4영역·라우트·`pages.json`, 3종 23개 조항,
공개/백엔드 버전과 조항의 완전 일치, 개인정보 안내와 선택동의 분리, 토큰·OTP,
템플릿 병합, 부분실패 복구, 비공개 Drive, A4 인쇄와 금지 문자열을 포함한다.

## 2. 메인 회귀검수

| 흐름 | 결과 | 비고 |
|---|---|---|
| 홈 → 데스크톱 코칭안내 드롭다운 | 통과 | 항목 5개·순서 확인 |
| 홈 → 전체 메뉴 → 코칭안내 | 통과 | 브랜드 다음 카테고리 |
| 홈 → 주요 바로가기 → 코칭안내 | 통과 | `/coaching/` |
| 홈 → 푸터 → 코칭계약서 | 통과 | `/coaching/agreement/` |
| 기존 DISC16 메뉴 런타임 주입 | 통과 | 로더 anchor 유지 |
| 기존 직업게임 메뉴 런타임 주입 | 통과 | 로더 anchor 유지 |
| 기존 COACHING FLEX MOVE 메뉴 주입 | 통과 | 로더 anchor 유지 |
| 기존 스마트스토어 1:1 코칭 링크 | 통과 | 상단·안내 CTA 유지 |

## 3. 계약 초안

| 시나리오 | 결과 | 비고 |
|---|---|---|
| 파라미터 없음 | 통과 | 유형 선택 화면 |
| `?type=life` | 통과 | 라이프 23조 |
| `?type=business` | 통과 | 2자·3자, 정보공유 |
| `?type=career` | 통과 | 커리어 23조 |
| 잘못된 `type` | 통과 | query 제거·선택 화면 |
| 필수항목 누락 | 통과 | 오류요약·필드 연결 |
| 종료일이 시작일보다 빠름 | 통과 | 오류 표시 |
| 회기 수 0·음수 | 통과 | 양의 정수 제한 |
| 금액 문자 입력 | 통과 | 원화 숫자 검증 |
| 긴 이름·조직명 | 통과 | maxlength·줄바꿈 |
| 3자→2자 전환 | 통과 | 확인 후 스폰서 정보 삭제 |
| 조항 전체 펼치기·접기 | 통과 | 키보드 가능 |
| 단계 왕복 | 통과 | 현재 문서 메모리에서 유지 |
| 새로고침·BFCache | 통과 | 입력·미리보기 초기화 |
| 초기화 | 통과 | DOM 포함 삭제 |
| 브라우저 저장소 | 통과 | local/session storage·cookie 0 |
| A4 인쇄·PDF 저장 | 통과 | 5쪽·정확한 A4·DRAFT |

브라우저 자동검수는 108/108을 통과했다. 생성한 표본 PDF는 75,144바이트,
5쪽 모두 `594.96 × 841.92pt` A4였고, 한글 Unicode 폰트가 내장됐다.
PDF form·JavaScript는 없으며 23개 조항, 개인정보 안내, 전자확인 전 영역을
텍스트 추출과 5쪽 렌더링으로 확인했다. 표본의 테스트 데이터와 PDF는 저장소에
포함하지 않는다.

## 4. 반응형·접근성

| 폭 | 결과 | 비고 |
|---:|---|---|
| 360px | 통과 | 가로 넘침 없음 |
| 390px | 통과 | 정보공유 카드 전환 |
| 768px | 통과 | 단계·본문 재배치 |
| 1024px | 통과 | 데스크톱 계약 흐름 |
| 1440px | 통과 | 최대폭·여백 유지 |

axe-core는 홈, 코칭안내, 유형 오류, 입력 오류, 라이프 미리보기, 비즈니스 3자,
비연결 sign 관문과 success 직접 진입 등 8개 상태에서 위반 0건이었다. 키보드
포커스, 단계 제목 포커스, 오류요약과 `aria-invalid`, 버튼 최소 높이 44px,
모바일 정보공유 카드와 `prefers-reduced-motion`도 통과했다.

## 5. 전자계약

실제 Apps Script `/exec` 배포 전에는 정적 sign 관문의 비연결 상태와 토큰 제거·origin 검증 코드만 검수한다. 정상 토큰·OTP·PDF·이메일 종단간 시험은 운영 Google 계정과 필수 운영값이 확정된 후 수행한다.

| 시나리오 | 결과 | 비고 |
|---|---|---|
| endpoint 미설정 | 통과 | `전자계약 시스템 연결 전` |
| fragment 없음·형식 오류 | 통과 | 존재 여부를 구분하지 않는 안내 |
| fragment 즉시 제거 | 통과 | URL·storage 미저장 |
| iframe `referrerpolicy=no-referrer` | 통과 | |
| 허용 origin·source 검증 | 통과 | 양방향 protocol 검사 |
| 외부 폰트·이미지·분석 제거 | 통과 | sign 경로 same-origin만 요청 |
| sign·success 검색 차단 메타 | 통과 | |
| DRAFT·미확정 운영값 발행 차단 | 소스 검수 통과 | 실제 계정 시험 전 |
| 토큰·OTP·중복제출·부분실패 복구 | 모의 검수 통과 | 실제 계정 시험 전 |
| 3자 목표공유 순서·역할별 표시본 | 모의 검수 통과 | 고객 확인 전 스폰서 대기 |
| 템플릿 marker 병합·비공개 PDF | 모의 검수 통과 | 실제 Drive 시험 전 |
| 정상·만료·회전·소비 토큰 | 운영 연동 후 | |
| OTP 오입력·만료·잠금 | 운영 연동 후 | |
| 고객·스폰서 순서 교차 | 운영 연동 후 | |
| PDF·이메일 재시도 | 운영 연동 후 | |
| 계약 관리 요청 | 운영 연동 후 | |

## 6. 알려진 운영 차단사항

- 실제 사업자·대표자·주소·연락처·과세/면세 안내 미확정
- 취소·노쇼·중도종료·환불 정책 미확정
- 보유기간 및 법정 보존 적용범위 미확정
- Google 처리위탁·국외이전 검토 미완료
- 개인정보 담당자 미확정
- 전자 청약철회·중도종료·변경·동의철회 백엔드 미배포
- 국내 법률전문가 최종 검토 미완료

위 항목이 해결되기 전 전자계약은 활성화하지 않는다.

## 7. 검수 범위의 한계

- Google Sheet, Drive 폴더, Apps Script 배포와 실제 PDF·이메일은 생성하지 않았다.
- Apps Script 구문·상태전이·중단 복구는 VM mock으로 검수했으며, Google 서비스의
  권한·할당량·메일 전달성·실제 리디렉션은 운영계정 종단간 검수 대상이다.
- 홈페이지와 신규 공개 페이지는 로컬 정적 서버에서 검수했다. PR이 아직 `main`에
  병합되지 않았으므로 배포 도메인의 공개 완료를 의미하지 않는다.
